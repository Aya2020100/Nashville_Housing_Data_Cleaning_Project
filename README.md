This project focuses on cleaning and preparing a raw dataset containing property information from the Nashville,
Tennessee real estate market. The raw data contained several inconsistencies, missing values, and unorganized formats.
Using SQL Server Management Studio (SSMS), 
This project contains structured SQL queries to transform this raw data into a clean, structured dataset 
that ready for accurate analysis.
